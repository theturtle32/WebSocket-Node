name: websocket-tests
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: 18.x

      - uses: actions/checkout@v2

      - run: npm install

      - run: npm run lint

      - run: npm run test

